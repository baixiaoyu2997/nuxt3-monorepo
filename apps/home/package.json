{
  "name": "@test/home",
  "version": "2.1.2",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:base && nuxi dev",
    "generate": "npm run prepare:base && nuxi generate",
    "prepare:base": "nuxi prepare ../../packages/base",
    "typecheck": "nuxi typecheck"
  },
  "license": "MIT",
  "dependencies": {
    "@test/base": "workspace:*",
    "@test/sdk": "workspace:*"
  }
}
